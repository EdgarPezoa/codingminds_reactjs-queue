const container: React.CSSProperties = {
  margin: 10,
  cursor: "pointer",
};

const containerHover: React.CSSProperties = {
  background: "red",
  margin: 10,
};

const text: React.CSSProperties = {
  marginLeft: 10,
};

const styles = {
  container,
  containerHover,
  text,
};

export default styles;
